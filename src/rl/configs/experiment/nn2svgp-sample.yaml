# @package _global_
defaults:
  - override /reward_model: nn2svgp
  - override /transition_model: nn2svgp

alg_name: "nn2svgp-sample-no-updates"
online_updates: false

transition_model:
  prediction_type: "SVGP"

reward_model:
  prediction_type: "SVGP"

agent:
  unc_prop_strategy: "sample"
