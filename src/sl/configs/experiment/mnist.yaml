# @package _global_
# defaults:
#   - override /agent: ddpg

dataset: "MNIST"
model_name: "SmallMLP"
lr: 1e-4
# n_epochs: 150
n_epochs: 5

prior:
  _target_: src.nn2svgp.priors.Gaussian
  # params: ???
  delta: 0.003
