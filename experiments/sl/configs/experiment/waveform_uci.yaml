# @package _global_
defaults:
  - override /network: si_mlp
  - override /dataset: waveform

lr: 1e-4
n_epochs: 10000

# sfr:
#   prior_precision: 0.001

early_stop:
  patience: 1000
  min_prior_precision: 0
