# @package _global_
# defaults:
#   - override /agent: ddpg

dataset: "FMNIST"
model_name: "CNN"
lr: 1e-4
n_epochs: 200

prior:
  _target_: src.priors.Gaussian
  # params: ???
  delta: 0.0013

hydra:
  launcher:
    timeout_min: 120
