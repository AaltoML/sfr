# @package _global_
defaults:
  - override /reward_model: cartpole
  - override /transition_model: svgp

# hydra:
#   sweeper:
#     params:
#       random_seed: 42,100,50,1,15

alg_name: "svgp-mean"
online_updates: false

agent:
  unc_prop_strategy: "mean"

wandb:
  run_name: ${alg_name}-${reward_model.sparse_threshold}-update_freq=${online_update_freq}
