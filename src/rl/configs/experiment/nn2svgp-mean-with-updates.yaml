# @package _global_
defaults:
  # - override /reward_model: nn2svgp
  - override /reward_model: cartpole
  - override /transition_model: nn2svgp

hydra:
  sweeper:
    params:
      random_seed: 42,100,50,1,15

alg_name: "nn2svgp-mean-with-updates"
online_updates: true

transition_model:
  prediction_type: "SVGPMeanOnly"
# reward_model:
#   prediction_type: "SVGPMeanOnly"
