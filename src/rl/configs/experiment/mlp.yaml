# @package _global_
defaults:
  - override /reward_model: nn2svgp
  - override /transition_model: nn2svgp

alg_name: "mlp-learn"
online_updates: false

transition_model:
  prediction_type: "NN"

reward_model:
  prediction_type: "NN"
