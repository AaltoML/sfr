# @package _global_
defaults:
  # - override /reward_model: sfr
  - override /reward_model: cartpole
  - override /transition_model: sfr

# hydra:
#   sweeper:
#     params:
#       random_seed: 42,100,50,1,15

alg_name: "sfr-mean-with-updates"
online_updates: true

transition_model:
  prediction_type: "SVGPMeanOnly"
# reward_model:
#   prediction_type: "SVGPMeanOnly"
