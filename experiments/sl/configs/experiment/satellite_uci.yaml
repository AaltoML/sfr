# @package _global_
defaults:
  - override /network: si_mlp
  - override /dataset: satellite

lr: 1e-4
n_epochs: 10000

sfr:
  prior_precision: 0.002

early_stop:
  patience: 1000
  min_prior_precision: 0
