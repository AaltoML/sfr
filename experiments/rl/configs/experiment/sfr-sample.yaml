# @package _global_
defaults:
  # - override /reward_model: sfr
  - override /reward_model: cartpole
  - override /transition_model: sfr

# hydra:
#   sweeper:
#     params:
#       random_seed: 42,100,50,1,15

alg_name: "sfr-sample-no-updates"
online_updates: false

transition_model:
  prediction_type: "SVGP"

# reward_model:
#   prediction_type: "SVGP"

agent:
  unc_prop_strategy: "sample"
# hydra:
#   launcher:
#     timeout_min: 1000
