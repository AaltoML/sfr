# @package _global_
defaults:
  - override /reward_model: nn2svgp
  - override /transition_model: nn2svgp

alg_name: "nn2svgp-mean-no-updates"
online_updates: false

transition_model:
  prediction_type: "SVGPMeanOnly"

reward_model:
  prediction_type: "SVGPMeanOnly"
